{
  "name": "@dexli/family",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared family infrastructure for dexli.dev tiny-tools — URL handoff builder + canonical sibling registry.",
  "scripts": {
    "test": "vitest run",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
